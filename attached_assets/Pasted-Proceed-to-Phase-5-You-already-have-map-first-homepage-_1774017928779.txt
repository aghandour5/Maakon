Proceed to Phase 5.

You already have:
- map-first homepage
- backend/data wiring
- detail views
- report flow
- polished homepage browsing UX
- post creation with privacy-safe location handling

Now implement the final MVP operational layer: basic moderation, NGO management, stale-post handling, and deployability cleanup.

## Phase 5 goal
Make Maakon operationally usable as an MVP by adding simple admin/moderation capabilities, keeping NGO data manageable, automatically handling stale posts, and preparing the app for a clean deployment on Replit.

## Scope for this phase
Focus only on:
1. basic admin/moderation backend support
2. NGO management support
3. stale/expired post handling
4. report review workflow foundations
5. final MVP cleanup for deployability
6. minimal admin-facing UI only if needed for testing/demo

Do not add full auth, full RBAC, chat, payments, notifications, Arabic UI, or advanced analytics yet.

---

## Requirements

### 1) Basic moderation support
Implement the minimum moderation tools needed for the MVP.

Moderation actions should support:
- hide post
- mark post as resolved
- mark post as expired
- restore post if hidden by mistake
- review reports
- add moderator/admin note

Use or refine the existing `admin_actions` table and related routes/services.

Suggested moderation states:
- active
- hidden
- resolved
- expired

Important:
- public map endpoints must continue showing only public-safe, active, non-expired content by default
- hidden/resolved/expired content must disappear from public map results

---

### 2) Reports review foundation
Use the existing `reports` table and improve the workflow.

Add support for:
- list reports
- view report details
- mark report as reviewed
- mark report as resolved
- optionally link moderation action taken

Suggested report statuses:
- open
- reviewed
- resolved
- dismissed

Keep this simple and MVP-friendly.

---

### 3) NGO management support
Implement basic NGO management so verified NGOs can be maintained.

Support:
- create NGO
- update NGO
- list NGOs
- mark NGO as verified
- unverify NGO if needed

Important:
- NGOs are public-facing organizations, so their public coordinates may remain exact
- keep NGO schema and responses clean and separate from user need posts
- homepage map should continue rendering NGOs correctly

If useful, add:
- NGO category / aid type
- NGO status (active/inactive)

But keep it minimal.

---

### 4) Stale / expired post handling
This is important for crisis-tech usefulness.

Implement automatic stale handling so outdated requests do not remain on the map forever.

Required behavior:
- posts already have `expiresAt`
- public endpoints must exclude expired posts by default
- add a clear backend utility/service for expiration filtering
- add a way to mark stale posts automatically or during fetch/query logic
- if useful, add a scheduled/manual cleanup function for demo/dev purposes

Recommended MVP behavior:
- if `expiresAt <= now`, treat the post as expired in public responses
- optionally update DB status via cleanup script/task
- support a lightweight “confirm still needed” concept later, but do not fully build it now

---

### 5) Admin/demo interface (minimal)
If needed for testing/demo, build a very small internal moderation page or dev-only panel.

This can be simple and does NOT need auth yet.

It should allow:
- viewing posts with status
- viewing reports
- triggering moderation actions
- viewing NGOs
- toggling NGO verification
- seeing expired vs active posts

Keep it clearly separated from the public UX.
Do not spend too much time on styling.

If a UI is too much for this phase, at minimum expose clean routes and simple test instructions.

---

### 6) Backend/API refinement
Add or refine the minimal routes needed for this phase.

Possible routes:
- GET /admin/posts
- PATCH /admin/posts/:id/status
- GET /admin/reports
- PATCH /admin/reports/:id
- POST /admin/actions
- POST /ngos
- PATCH /ngos/:id

You may adjust names to match the existing project conventions.

Important:
- keep public and admin responses separate where needed
- public post serializers must still never expose `privateLat`, `privateLng`, or `exactAddressPrivate`

---

### 7) Seed/demo readiness
Improve seed data so the MVP demo is strong.

Include:
- mix of active, expired, resolved, and hidden test posts
- multiple reports in different statuses
- 5+ NGOs with verified and non-verified cases if supported
- useful variety across Lebanon

Make sure the app still feels alive immediately after setup.

---

### 8) Deployment cleanup
Prepare the project for a clean MVP deployment on Replit.

Do a final cleanup for:
- environment variables
- startup scripts
- seed/migration instructions
- API/frontend run commands
- error handling
- empty/loading states where still missing
- README or run instructions
- remove obvious dead code or duplicate mock paths if backend integration is complete

Make sure the app can be run and demoed reliably.

---

### 9) Quality rules
- Keep code modular and readable
- Do not overengineer
- Preserve privacy protections strictly
- Keep public map behavior stable
- Prefer simple, maintainable implementation over complex architecture

---

## Output required
Give me:
1. exact files changed
2. code for each changed file
3. explanation of moderation/stale-post handling
4. how to run/test Phase 5 in Replit
5. a final MVP status summary:
   - what is complete
   - what is still out of scope
   - what should be Phase 6 if we continue

## Important
- Keep the homepage map-first
- Keep privacy protections strict
- Do not expose exact private location in any public/admin response unless explicitly intended for internal-only logic
- Finish only Phase 5 and stop