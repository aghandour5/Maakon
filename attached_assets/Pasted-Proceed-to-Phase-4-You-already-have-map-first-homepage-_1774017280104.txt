Proceed to Phase 4.

You already have:
- map-first homepage
- backend/data wiring
- detail views
- report flow
- polished homepage browsing UX

Now implement safe MVP post creation for need and offer posts.

## Phase 4 goal
Allow users to submit new need posts and offer posts in a mobile-first way, while enforcing privacy-safe location handling and keeping the MVP simple.

## Scope for this phase
Focus only on:
1. post creation UI
2. metadata-driven form fields
3. safe location input and public-location generation
4. backend validation and create-post flow
5. success/error UX
6. minimal frontend integration so newly created posts can appear on the map

Do not add auth, chat, payments, notifications, Arabic UI, or admin dashboard yet.

---

## Requirements

### 1) Post creation entry points
Add a clear way to create:
- a need post
- an offer post

This can be via:
- floating action button
- prominent mobile-first CTA
- segmented create flow
- or a create modal/page/sheet

The UX should make it obvious whether the user is creating:
- Need Help
- Offer Help

Keep it simple and mobile-first.

---

### 2) Form structure
Implement a post creation form for both post types.

Common fields:
- postType
- title
- category
- description
- governorate
- district
- contactMethod
- contactInfo
- expiresAt (or expiration preset)
- providerType if relevant

Need-post specific:
- urgency
- optional private exact address / precise location input for internal use only

Offer-post specific:
- provider type
- whether location is public-safe or should remain approximate

Use metadata-driven options wherever possible:
- categories
- governorates
- districts
- urgency options
- provider types if modeled

Use the existing metadata endpoint or extend it if needed.

---

### 3) Safe location handling (critical)
This is mandatory.

For need posts:
- never place exact user GPS on the public map
- generate/store `publicLat` and `publicLng` from a safe approximate district-level location
- if exact/private location is collected, store it privately only
- exact/private location must never be returned in public endpoints

For offer posts:
- allow approximate public location by default
- only expose exact public coordinates if explicitly safe and intended for public display
- if not explicitly public, also derive approximate public coordinates

Implementation guidance:
- use district centroid mapping or a safe server-side approximation strategy
- do not trust frontend-submitted public coordinates blindly
- server should compute/validate the public-safe coordinates

Add comments in code explaining the privacy logic.

---

### 4) Backend create-post flow
Implement or refine POST /posts.

Server-side responsibilities:
- validate payload
- enforce allowed enums/fields
- enforce privacy-safe coordinate generation
- set default status to active where appropriate
- set timestamps
- set expiry
- return a safe public response shape

If not already present, create:
- validation schema
- request/response mapper
- public-safe serializer

The create endpoint must never echo back private coordinates in a public response.

---

### 5) Frontend UX
The create flow should include:
- good mobile form layout
- clear labels
- validation messages
- loading state on submit
- success state
- error state
- cancel/back behavior

After successful creation:
- either return to homepage map and show the new post
- or show a success screen and then navigate back to map
- refresh/reload relevant map data

Keep the UX calm and practical.

---

### 6) Expiration behavior
Creation flow must support expiry.

For MVP, you may use one of these:
- preset durations (24h, 48h, 72h)
- or a simple datetime input

Recommended:
- presets for simplicity

Default behavior:
- posts should expire automatically
- public map only shows active + non-expired posts

---

### 7) API/frontend integration
Add whatever minimal support is needed for:
- create post form submission
- metadata-driven selects
- safe returned response types
- homepage refresh after creation

Do not redesign the whole app.

---

### 8) Quality rules
- Keep code modular and readable
- Use TypeScript types cleanly
- Reuse components where sensible
- Do not overengineer
- Prioritize safety, simplicity, and maintainability

---

## Output required
Give me:
1. exact files changed
2. code for each changed file
3. explanation of safe location handling implementation
4. how to run/test Phase 4 in Replit
5. what remains for Phase 5

## Important
- Keep homepage map-first
- Keep privacy protections strict
- Do not expose exact private location in public flows
- Finish only Phase 4 and stop